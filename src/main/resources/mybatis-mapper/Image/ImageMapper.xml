<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petToyShop.image.mapper.ImageMapper">

    <insert id="uploadImage" parameterType="com.example.petToyShop.image.vo.Image">
        INSERT INTO image (id, title, description, name, file_path)
        VALUES (image_seq.NEXTVAL, #{title}, #{description}, #{name}, #{file_path})
    </insert>

</mapper>

